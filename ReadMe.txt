백준 5026 문제 풀이 중입니다.

문제 풀이를 완료했습니다.

해당 코드는 문제를 풀긴 했지만 스파케기 코드이므로 개선이 무조건적으로 필요합니다.

먼저 scanf 함수를 통해 문자열을 입력 받고 해당 코드가 "N=NP"면 "skipped"를 출력하고 아닐 경우

해당 입력은 더하기 연산이므로 sscanf 함수로 입력된 문자열 n을 다시 읽어 더하기 연산을 해줍니다.

참고로 scanf 함수에서 "%d+%d"로 매개변수를 줄 경우 n1+n2 형식으로 입력이 주어진다 가정하여 n1, n2에 정수를 입력해 줍니다.
